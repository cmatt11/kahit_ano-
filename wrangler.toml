# Deploys this folder as a static-assets-only Cloudflare Worker.
# The site is just HTML/CSS/JS, so there is no server-side entry point.
# Workers Builds runs `npx wrangler deploy`, which reads this file.
name = "kahitanopage"
compatibility_date = "2024-11-01"

[assets]
directory = "./"
